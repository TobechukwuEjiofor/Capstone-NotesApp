services:
  frontend:
    image: ${REGISTRY}/notes-frontend:${TAG}
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    image: ${REGISTRY}/notes-backend:${TAG}
    environment:
      DB_HOST: ${DB_HOST}
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: notesdb
      DB_SSL: "true"
    restart: unless-stopped